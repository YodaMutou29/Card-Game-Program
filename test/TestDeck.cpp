#include "gtest/gtest.h"
#include "Deck.h"
#include "Game.h"
//#include <iostream>
//using namespace std;
/*
TEST(TestDeck, BuildNormalDeck) {
  Deck* normal = new Deck(Game::GOFISH);
  EXPECT_EQ(52, normal->getNumberOfCards());
  delete normal;
}

TEST(TestDeck, BuildUnoDeck) {
  Deck* uno = new Deck(Game::UNO);
  EXPECT_EQ(108, uno->getNumberOfCards());
  delete uno;
}

TEST(TestDeck, DISABLED_TopCard) {
  Deck* deck = new Deck(Game::GOFISH);
}

TEST(TestDeck, DISABLED_Shuffle) {
  Deck* deck = new Deck(GOFISH);
  vector<Card> temp = getCurrentDeck();
  shuffleDeck();
  EXPECT_FALSE(temp == getCurrentDeck());
}
*/
